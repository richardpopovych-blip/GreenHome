# 🧪 TestCases.md

## 📘 Мета роботи
Мета цього практичного завдання — створити 2–3 тест-кейси для проєкту **GreenHome** з використанням фреймворку **JUnit 5**.  
Тестування проводиться для базового функціоналу системи авторизації користувачів: перевірка входу, реєстрації та обробки дублікатів.

## 🧱 Контекст проєкту
Проєкт **GreenHome** — це система моніторингу енерговитрат у розумному будинку. Для доступу до персональних даних користувач повинен пройти автентифікацію.  
Основні методи, що тестуються:
- `login(email, password)` — авторизація користувача;
- `register(email, password)` — реєстрація нового користувача;
- `UserAlreadyExistsException` — виняток при спробі створення дубліката користувача.

---

## 🧩 Тест-кейси

### ✅ Тест-кейс 1. Успішна авторизація користувача

**Мета:** перевірити, що існуючий користувач може успішно авторизуватися у системі.  
**Передумови:** у базі даних існує користувач `testuser@gmail.com` з паролем `123456`.

| Поле | Опис |
|------|------|
| **Назва тесту** | Успішна авторизація користувача |
| **Кроки виконання** | 1. Викликати метод `login("testuser@gmail.com", "123456")` <br> 2. Перевірити, чи метод повертає позитивний результат |
| **Очікуваний результат** | Метод повертає `true` або об’єкт із токеном доступу |
| **Фактичний результат** | Метод повернув `true`, автентифікація пройдена |
| **Статус** | ✅ Pass |

```java
@Test
void testSuccessfulLogin() {
    boolean result = authService.login("testuser@gmail.com", "123456");
    assertTrue(result, "Користувач повинен успішно авторизуватись");
}
```

---

### ❌ Тест-кейс 2. Авторизація з невірним паролем

**Мета:** перевірити, що система не надає доступу при введенні неправильного паролю.  
**Передумови:** існує користувач `testuser@gmail.com`.

| Поле | Опис |
|------|------|
| **Назва тесту** | Авторизація з невірним паролем |
| **Кроки виконання** | 1. Викликати метод `login("testuser@gmail.com", "wrongpass")` <br> 2. Перевірити результат виконання |
| **Очікуваний результат** | Метод повертає `false` або викидає `AuthenticationException` |
| **Фактичний результат** | Метод повернув `false` |
| **Статус** | ✅ Pass |

```java
@Test
void testLoginWithInvalidPassword() {
    boolean result = authService.login("testuser@gmail.com", "wrongpass");
    assertFalse(result, "Авторизація з невірним паролем має бути відхилена");
}
```

---

### ⚠️ Тест-кейс 3. Реєстрація користувача, який вже існує

**Мета:** перевірити, що система не дозволяє зареєструвати користувача з існуючим email.  
**Передумови:** у базі вже є користувач `testuser@gmail.com`.

| Поле | Опис |
|------|------|
| **Назва тесту** | Реєстрація дубліката користувача |
| **Кроки виконання** | 1. Викликати метод `register("testuser@gmail.com", "123456")` <br> 2. Очікувати виняток `UserAlreadyExistsException` |
| **Очікуваний результат** | Метод викидає виняток `UserAlreadyExistsException` |
| **Фактичний результат** | Виняток отримано |
| **Статус** | ✅ Pass |

```java
@Test
void testDuplicateUserRegistration() {
    assertThrows(UserAlreadyExistsException.class, () -> {
        authService.register("testuser@gmail.com", "123456");
    }, "Система повинна заборонити створення дубліката користувача");
}
```

---

### 🆕 Тест-кейс 4. Реєстрація нового користувача

**Мета:** перевірити, що користувач з новим email може успішно зареєструватися.  
**Передумови:** користувач з email `newuser@gmail.com` не існує в базі.

| Поле | Опис |
|------|------|
| **Назва тесту** | Успішна реєстрація нового користувача |
| **Кроки виконання** | 1. Викликати метод `register("newuser@gmail.com", "pass123")` <br> 2. Перевірити, чи користувач з'явився в базі |
| **Очікуваний результат** | Користувач створюється, метод повертає `true` |
| **Фактичний результат** | Користувач успішно створений |
| **Статус** | ✅ Pass |

```java
@Test
void testRegisterNewUser() {
    boolean result = authService.register("newuser@gmail.com", "pass123");
    assertTrue(result, "Користувач повинен бути успішно створений");
}
```

---

## 📋 Висновки
У результаті виконання тестів було перевірено базові сценарії роботи системи автентифікації:  
- Коректна авторизація існуючого користувача;  
- Відмова у вході при невірних даних;  
- Запобігання створенню дубліката облікового запису;  
- Можливість успішної реєстрації нового користувача.

Всі тест-кейси пройшли успішно. Функціонал модуля авторизації відповідає вимогам.